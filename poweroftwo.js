//Write a method that takes in a number and returns true if it is a power of 2. Otherwise, return false
 // steps needed, 1. input - number, 
 //2. output is boolean (true if a power of 2 otherwise false)

 function isPowerOftwo(number){
     if(Math.pow(number) === 2){
         return true;
     }
 }
 console.log(isPowerOftwo(64)): // expect